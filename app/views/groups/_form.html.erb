<%= form_for @group do |d| %>
<% if @group.errors.any? %>
<ul>
  <% @group.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
</ul>
<% end %>
標題
<%= f.text_field :title %>
<br>
敘述
<br>
<%= f.text_area :description %>
<br>

<%= f.submit "submit", disable_with => 'Submiting...' %>
<% end %>
